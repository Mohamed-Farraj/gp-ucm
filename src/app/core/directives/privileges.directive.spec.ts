import { PrivilegesDirective } from './privileges.directive';

describe('PrivilegesDirective', () => {
  it('should create an instance', () => {
    const directive = new PrivilegesDirective();
    expect(directive).toBeTruthy();
  });
});
